project_id = "wxkiqfakjtjciybeqrjh"

# Stripe webhook — must not require Supabase JWT (Stripe calls it directly).
# Signature is verified inside the function via STRIPE_WEBHOOK_SECRET.
[functions.stripe-webhook]
verify_jwt = false
